# AlphaGenome (optional)

Copy to `.env` in this track root or `pilot_scaffold/` (gitignored):

```
ALPHAGENOME_API_KEY=
```

Do not commit the filled `.env`. Agents should load via `pilot_scaffold/load_project_env.py` and never print the key.
