# Data assets — `se_llps`

Committed under `data/input/`: ENCODE/JSON extracts, peak BED subsets, chain files needed to reproduce published nulls.

**Not** treated as git secrets, but do not re-commit multi-GB raw BAMs if you fetch them locally.

If a fetch script expects a file that is missing, re-run the matching `scripts/fetch_*.py`.
